# Compiled packages that should be installed via conda
# to avoid OpenMP library conflicts
pytorch
torchvision
torchaudio
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
# open3d>=0.17.0  # Optional - install manually if needed for Point2CAD normal estimation

# CAD packages - these share OCCT (OpenCASCADE) as a dependency
# IMPORTANT: pythonocc-core and cadquery must use compatible OCCT versions!
# - pythonocc-core=7.8.1 uses occt=7.8.1
# - cadquery=2.6.1 uses ocp>=7.8.1 which also uses occt=7.8.1
# - occt-rt uses occt=7.8.1 for BVH raytracing
# Install together: conda install -c pozzettiandrea -c cadquery -c conda-forge pythonocc-core=7.8.1 cadquery=2.6.1 occt-rt
pythonocc-core=7.8.1  # Python bindings for OpenCASCADE (CAD kernel)
cadquery=2.6.1        # CadQuery for executing CAD-Recode generated code (requires -c cadquery channel)
occt-rt>=1.1.0        # BVH raytracer for OpenCASCADE (requires -c pozzettiandrea channel)

# pytorch3d  # For farthest point sampling - install via pip with wheel URL
