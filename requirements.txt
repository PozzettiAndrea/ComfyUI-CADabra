gmsh>=4.11.0
numpy>=1.24.0
torch>=2.0.0
transformers>=4.30.0
trimesh>=3.20.0
pygltflib
scikit-image>=0.21.0

# Mesh Reconstruction Pipeline Dependencies (Phase 1 MVP)
open3d>=0.17.0           # Point cloud processing and visualization
scikit-learn>=1.3.0      # KMeans clustering for segmentation
scipy>=1.11.0            # Spatial operations and optimization
pyransac3d>=0.5.0        # Primitive fitting (RANSAC-based) - optional but recommended
accelerate

# Note: B-rep generation uses gmsh.model.occ (already included above)
# This eliminates the need for pythonocc-core in Phase 1!

# Optional: Advanced CAD operations (Phase 2+)
# pythonocc-core provides advanced surface operations if needed:
#   conda install -c conda-forge pythonocc-core=7.9.0
# Used by CAD_Convert_Format node for advanced format conversion

# Point2CAD Dependencies (CVPR 2024)
geomdl>=5.3.0            # NURBS/B-spline operations for freeform surfaces
pyvista>=0.43.0          # Advanced visualization and mesh processing
rtree>=1.0.0             # Spatial indexing for surface intersection
cgal>=0.9.0              # Mesh clipping/trimming for topology extraction (replaces pymesh2)

# CAD-Recode Dependencies (ICCV 2025)
# cadquery - install via conda: conda install -c conda-forge cadquery
# pytorch3d>=0.7.0       # For farthest point sampling (optional, falls back to random)
# Install pytorch3d with: pip install pytorch3d -f https://dl.fbaipublicfiles.com/pytorch3d/packaging/wheels/py310_cu118_pyt201/download.html

# SECAD-Net Dependencies (sketch-extrude CAD reconstruction)
PyMCubes>=0.1.4          # Marching cubes for mesh extraction from occupancy grids
gdown>=4.7.0             # Google Drive downloads for pretrained models

# Future Dependencies (Phase 2+):
# torch-geometric>=2.4.0   # For PointNet++ graph operations
# quadwild                 # Quad remeshing (requires C++ compilation)