You are generating a new ComfyUI extension called ComfyUI-CADabra.
Produce all code, scripts, tests, and files needed for a clean install.
Do NOT hallucinate model APIs — wrap ML models in stubs that I will fill later.
Follow all instructions below exactly.

📦 1. Produce the following files:
A) nodes.py

Implement all backend nodes:

Node 1: CAD_Load_Gmsh

Input: STEP / IGES / BREP file path

Use gmsh Python API to load the CAD

Output: "CAD_MODEL" + "DEBUG_TEXT"

Handle missing gmsh via try/except

One-time gmsh initialization per session (global guard)

Node 2: CAD_Mesh_Gmsh

Inputs:

"CAD_MODEL"

mesh dimension (2/3)

element size float

optimize flag

Produce "MESH" = (vertices, faces)

Output a "DEBUG_TEXT"

CPU-only compatible

Node 3: ML_SurfaceRecon

JUST MAKE A PLACEHOLDER FOR NOW

Models supported (via stubs only):

hustvl/surface-recon

threedvision/point2surf

zju3dv/patchnetsurface

kaist-3d/point2shape

Stub API (do NOT hallucinate details):

def load_surface_model(name): ...
def run_surface_inference(model, vertices, faces): ...

Node 4: ML_FeatureDetection

JUST MAKE A PLACEHOLDER FOR NOW

Models (via stubs only):

autodesk/feature-detection-cad

hustvl/cad-features

Stub API:

def load_feature_model(name): ...
def run_feature_detection(model, vertices, faces): ...


Outputs:

"FEATURE_MAP"

"DEBUG_TEXT"

Node 5: CAD_Viewer

A frontend viewer node (OUTPUT_NODE = True)

Minimal HTML/JS using OCC.js placeholder

Provide a UI wrapper via the ui() method

Should accept:

original CAD file

reconstructed surfaces

mesh geometry

Viewer implementation can be stubbed

All nodes must:

follow ComfyUI INPUT_TYPES, RETURN_TYPES, FUNCTION, CATEGORY="CADMancer"

be clean, PEP8, class-based

include graceful error messages

not require GPUs

At bottom:

Provide the correct ComfyUI NODE_CLASS_MAPPINGS + NODE_DISPLAY_NAME_MAPPINGS.

📜 B) requirements.txt

Include only necessary dependencies:

gmsh
numpy
torch
transformers
trimesh


(Claude may add a few more if needed — but keep it lean.)

No GPU-only libs.

🧪 C) tests_cpu/test_gmsh_nodes.py

Simple CPU-only pytest suite:

Test that CAD_Load_Gmsh loads a tiny STEP file (mock or synthetic)

Test that CAD_Mesh_Gmsh returns non-empty vertices/faces

Test that ML stub nodes run without error using dummy data

Use ONLY CPU

Use lightweight synthetic geometry (e.g., single triangle, parametric box)

All tests should pass without external downloads.

🛠 D) install.py

A Python script executed by a user to:

Check Python version

Install requirements.txt

Optionally download HF model folders (stubbed)

Create directory structure:

ComfyUI-CADMancer/
    models/surface/
    models/features/


Print success message

No hallucinated URLs.
Use placeholder comments for model downloads.

🚦 E) prestartup.py

This file runs automatically before ComfyUI loads.

It must:

Check that gmsh can import (warn, not crash)

Pre-create cache folders

Print a colored banner: “CADMancer loaded (CPU-safe)”

Not import any heavy ML models

Not run anything GPU-dependent

📘 F) README.md

Write a clean readme describing:

What CADMancer does

Node descriptions

How to install (via install.py or pip)

Known issues

Roadmap (surface recon → CSG → parametric CAD)

Make it professional but fun.

🧩 2. File structure Claude should output:
ComfyUI-CADMancer/
│
├── nodes.py
├── requirements.txt
├── install.py
├── prestartup.py
├── README.md
│
└── tests_cpu/
    └── test_gmsh_nodes.py

🧠 3. Additional rules

All ML operations must be stubbed (I will fill them).

Do not fabricate APIs for HuggingFace models.

Viewer node JS can be placeholder code.

All gmsh calls must be CPU-only compatible.

No CUDA, no GPU ops.

🚀 Now generate ALL files exactly as described, inside markdown code blocks.